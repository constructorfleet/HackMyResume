
module.exports = {
  toLowerCase: str => str.toString().toLowerCase()
};

// Handlebars.registerHelper("toLowerCase", (text = "") => {
//   return new Handlebars.SafeString(text.toLowerCase());
// });